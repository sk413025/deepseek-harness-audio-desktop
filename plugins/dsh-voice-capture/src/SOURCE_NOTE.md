# Source provenance

The `src/` files here were extracted from `sourcesContent` in the shipped `lib/client.js.map` of dsh-voice-capture 0.3.5 (package sha256 `79cfe8f2…`). They are byte-for-byte the sources that bundle was built from.

# Source provenance

The `src/` files here were extracted from `sourcesContent` in the shipped `lib/client.js.map` of dsh-voice-capture 0.3.8 (package sha256 `683a4cb8…`). They are byte-for-byte the sources that bundle was built from.

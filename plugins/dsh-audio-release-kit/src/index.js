// Host half of dsh-audio-release-kit: intentionally empty. The package exists to ship read-only
// agent presets (see package.json `dsh.agentPresets`) with the local audio Desktop build.
export const name = 'dsh-audio-release-kit'

export function apply() {}
